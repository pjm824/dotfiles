bindkey "^P" up-line-or-search    # ctrl-p for up
bindkey "^N" down-line-or-search  # ctrl-n for down
bindkey "^A" vi-beginning-of-line # ctrl-a for beginning of line
bindkey "^E" vi-end-of-line       # ctrl-e for end of line
